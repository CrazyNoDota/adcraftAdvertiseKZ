# Optional. If unset, the AI mockup falls back to a free Pollinations.ai render
# and then to a canvas-based placeholder composite. Wire a real model later.
POLLINATIONS_BASE_URL=https://image.pollinations.ai/prompt

# Optional. Future: Replicate / fal.ai for SDXL+ControlNet image-to-image.
# REPLICATE_API_TOKEN=
# FAL_KEY=
